__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/
.env
instance/
{% if database == "sqlite" %}
*.db
{% endif %}
{% if css == "tailwind" %}
# Built by the Tailwind CLI (see README.md's "Styling" section) — the
# source of truth is static/css/input.css, not this file.
src/{{ package_name }}/static/css/style.css
{% endif %}
