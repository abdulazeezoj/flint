# AGENTS.md

Context for AI coding agents working in this repository.

## Project

{{ project_name }} — a FastAPI application generated by [flint](https://github.com/abdulazeezoj/flint).

## Setup

```
uv sync
```

## Run

```
uv run fastapi dev src/{{ package_name }}/main.py
```

## Test

```
uv run pytest
```

## Layout

- `src/{{ package_name }}/main.py` — the FastAPI app (`app`) and its routes.
- `tests/` — pytest tests; use `fastapi.testclient.TestClient` against
  `{{ package_name }}.main.app`.
{% if config %}
- `src/{{ package_name }}/config.py` — `Settings` (`pydantic-settings`),
  loaded from `.env`. Add new config fields there, not as scattered
  `os.environ` reads.
{% endif %}

## Conventions

- Dependencies are managed with `uv` via `pyproject.toml` — don't hand-edit
  `uv.lock`; run `uv add <package>` / `uv remove <package>` instead.
- Keep route handlers typed; return plain dicts or Pydantic models, not
  raw strings.
