FROM ghcr.io/astral-sh/uv:python3.11-bookworm-slim

WORKDIR /app

ENV UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy \
    UV_PROJECT_ENVIRONMENT=/app/.venv

COPY pyproject.toml uv.lock* ./
RUN uv sync --no-install-project --no-dev

COPY . .
RUN uv sync --no-dev
{% if css == "tailwind" %}
RUN uv run tailwindcss \
    -i src/{{ package_name }}/static/css/input.css \
    -o src/{{ package_name }}/static/css/style.css \
    --minify
{% endif %}

ENV PATH="/app/.venv/bin:$PATH"

EXPOSE 8000

CMD ["fastapi", "run", "src/{{ package_name }}/main.py", "--host", "0.0.0.0", "--port", "8000"]
