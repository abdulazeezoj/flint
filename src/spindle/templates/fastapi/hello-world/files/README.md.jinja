# {{ project_name }}

A FastAPI project generated by [spindle](https://github.com/abdulazeezoj/spindle).

## Run

```
uv run fastapi dev src/{{ package_name }}/main.py
```

Then open <http://127.0.0.1:8000> — you should see `{"message": "Hello, World!"}`.

## Test

```
uv run pytest
```
{% if config %}
## Configuration

Settings are loaded from `.env` via `pydantic-settings` (see
`src/{{ package_name }}/core/config.py`). `.env` is already git-ignored;
`.env.example` is the checked-in reference of what vars exist.
{% endif %}
{% if docker %}
## Docker

```
docker build -t {{ slug }} .
docker run -p 8000:8000 {{ slug }}
```
{% endif %}
## Layout

```
src/{{ package_name }}/   application package
tests/                    tests
AGENTS.md                 project context for AI coding agents
{% if config %}
.env                      local configuration (git-ignored)
.env.example              checked-in reference of what vars exist
{% endif %}
{% if docker %}
Dockerfile                container build
{% endif %}
```
