# AGENTS.md

Context for AI coding agents working in this repository.

## Project

{{ project_name }} — a FastAPI REST API generated by [spindle](https://github.com/abdulazeezoj/spindle).

## Setup

```
uv sync
```

## Run

```
uv run fastapi dev src/{{ package_name }}/main.py
```
{% if migrations %}
Apply migrations first: `uv run alembic upgrade head`.
{% endif %}
{% if worker == "taskiq" %}
Run the worker (separate process): `uv run taskiq worker {{ package_name }}.worker:broker --app-dir src`.
{% elif worker == "celery" %}
Run the worker (separate process): `PYTHONPATH=src uv run celery -A {{ package_name }}.worker.celery_app worker --loglevel=info`.
{% endif %}

## Test

```
uv run pytest
```

## Layout

Two fixed entrypoints, two "add a resource here" folders, one folder for
shared infrastructure — the rest is unopinionated (borrowed from how
Next.js only enforces structure for routing, nothing else):

- `src/{{ package_name }}/main.py` — **the** FastAPI entrypoint: `app`,
  lifespan, mounted routers. Don't rename or move it.
- `src/{{ package_name }}/routes/` — **one module per HTTP resource.**
  `items.py` is the example; a new resource is a new file here plus one
  `app.include_router(...)` line in `main.py`.
{% if worker != "none" %}
- `src/{{ package_name }}/worker.py` — **the** {{ worker }} entrypoint:
  broker/app setup, importing every `tasks/` module so the worker
  process can discover them. Don't rename or move it.
- `src/{{ package_name }}/tasks/` — **one module per background job.**
  `example.py` is the demo; a new task is a new file here plus one
  import line in `worker.py` (see the comment there for why).
{% endif %}
- `src/{{ package_name }}/core/` — shared infrastructure every
  route/task might depend on: `config.py` (`Settings`, always), {% if database != "none" %}`db.py`
  (async engine/session), {% endif %}{% if redis %}`redis.py` (the `redis_client`), {% endif %}and
  the natural home for anything similar later (e.g. a scheduler). Add
  new config fields to `Settings`, not scattered `os.environ` reads.
- `src/{{ package_name }}/schemas.py` — Pydantic request/response
  models.{% if database != "none" %} Not enforced the way `routes/`/`tasks/`/`core/` are — a `schemas/`
  folder is a reasonable next step once there's more than one resource.{% endif %}
{% if database != "none" %}
- `src/{{ package_name }}/models.py` — {{ orm }} models. Same story as
  `schemas.py`: fine as one file for one resource, free to become
  `models/` later.
{% endif %}
{% if migrations %}
- `alembic/` — migrations. New model → `uv run alembic revision
  --autogenerate -m "..."`, review the generated file, then `uv run
  alembic upgrade head`.
{% endif %}
- `tests/` — pytest tests{% if database != "none" %} against an
  isolated in-memory SQLite database via a `get_session` override in
  `tests/conftest.py` — the production database is never touched by
  the test suite{% endif %}.

## Conventions

- Dependencies are managed with `uv` via `pyproject.toml` — don't hand-edit
  `uv.lock`; run `uv add <package>` / `uv remove <package>` instead.
- Keep route handlers typed; return plain dicts or Pydantic models, not
  raw strings.
- New HTTP endpoints go in `routes/`, new background jobs go in
  `tasks/`, new shared infrastructure goes in `core/` — don't put any of
  these in `main.py`/`worker.py` themselves, which stay thin entrypoints.
{% if database != "none" %}
- New endpoints that touch the database take `session` via
  `Depends(get_session)` — don't create engines/sessions ad hoc.
{% endif %}

## Agent skills

Deeper, library-specific reference material lives in `.agents/skills/`
— start with `.agents/skills/README.md` for the full index. This
project ships:

- `.agents/skills/fastapi/SKILL.md` — routing, dependency injection,
  request/response models, lifespan, testing conventions.
- `.agents/skills/pydantic-settings/SKILL.md` — configuration via
  `Settings`.
- `.agents/skills/pytest/SKILL.md` — this project's testing
  conventions (isolated database, async fixtures).
{% if orm == "sqlmodel" %}
- `.agents/skills/sqlmodel/SKILL.md` — model definition, async
  sessions, query patterns.
{% elif orm == "sqlalchemy" %}
- `.agents/skills/sqlalchemy/SKILL.md` — declarative models, async
  sessions, query patterns.
{% endif %}
{% if migrations %}
- `.agents/skills/alembic/SKILL.md` — the migration workflow, and why
  the database is deliberately **not** auto-created on startup.
{% endif %}
{% if worker == "taskiq" %}
- `.agents/skills/taskiq/SKILL.md` — task definitions, the two-process
  model, and the task-discovery gotcha (read this before adding a task).
{% elif worker == "celery" %}
- `.agents/skills/celery/SKILL.md` — task definitions, the two-process
  model, and the task-discovery gotcha (read this before adding a task).
{% endif %}
{% if redis %}
- `.agents/skills/redis/SKILL.md` — caching client usage, and why
  cache invalidation on writes isn't automatic here.
{% endif %}
